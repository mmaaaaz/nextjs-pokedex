import { Pokemon2 } from "@/types/Poke2"

const PokemonStats = ({ pokemon }: { pokemon: Pokemon2 }) => {
  // const normalizedPokemon = normalizePokemon({ pokemon })
  // const len = pokemon.types.length

  // const gridColLength = `grid-cols-${len}`

  return (
    <div className="flex w-full flex-col items-start justify-start overflow-y-auto"></div>
  )
}

export default PokemonStats
